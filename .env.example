# Compose-level secrets (docker-compose reads these)
POSTGRES_PASSWORD=change-me-strong
# Public domain for auto-HTTPS, e.g. docarchive.example.com
# Use ":80" for local testing without TLS.
SITE_ADDRESS=:80
