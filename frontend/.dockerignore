# host deps must never shadow the ones installed in the build stage
node_modules/
**/node_modules/

# build output (rebuilt inside the image)
dist/
.vite/

# secrets / local env
.env
.env.*
!.env.example

# vcs / editors / docs
.git/
.gitignore
.dockerignore
Dockerfile
*.md
.idea/
.vscode/
.DS_Store

# logs
npm-debug.log*
*.log
